/**
 * poker.Pot is the model for the main and side pots of a poker hand.
 */
;(function (root) {
  'use strict'

  var NS = root.DISBRANDED.poker
  
  NS.Pot = function () {
    //
  }
  
  NS.Pot.prototype = {
    //
  }
}(this));