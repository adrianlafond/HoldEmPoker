/**
 * Static methods attached to Poker.Hand.
 */

/**
 *
 */
Hand.findFlush = function (cards, low) {
  var flush = null
  if (cards.length < 5) {
    return flush
  }
  low = !!low



  return flush
}



